import { Modifier } from "../index.js";

export const ModBinaryEncode: Modifier<[]> = {
    name: "Binary Encode",
    options: [],
    run: (s) => s,
};

export const ModBinaryDecode: Modifier<[]> = {
    name: "Binary Decode",
    options: [],
    run: (s) => s,
};
